import 'package:flutter/material.dart';
import 'Components/body.dart';

class LoginScreenC extends StatelessWidget {
  const LoginScreenC({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}